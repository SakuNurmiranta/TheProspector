## DESIGN REFERENCE: RESONANCE SYSTEM

Each GameEntity has the trait "ResonanceTag" which gets updated after each round by a mechanism that tracks what Tag the entity has mostly been influenced by. Because each entity has the tag, it is the tag most readily available in situations where tags are used, such as when bands gestate music or when an individual band member interacts with another entity.