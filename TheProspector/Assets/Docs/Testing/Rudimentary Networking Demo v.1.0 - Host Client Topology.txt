Note: This was the third test run

=====================================================================
1. SESSION & TOPOLOGY
=====================================================================

Feature                                               | Status | Notes
---------------------------------------------------------------------
2–4 players can join same session (host + RELAY)      | true   | Tried with 3 players
Distributed Authority topology selectable             | false  | not implemented yet
Session starter becomes temporary Keeper              | true   | 


=====================================================================
2. PLAYER ENTITIES (score / exhaustion / isActive)
=====================================================================

Feature                                               | Status | Notes
---------------------------------------------------------------------
Players spawn with default traits                     |        |
(score=0, exhaustion=false, isActive=true)            | true   | 
Player trait sync works for score/exhaustion/isActive | true   | 
lastResolvedRound identical on all clients at start   | true   | 


=====================================================================
3. TURN / ROUND / YEAR SYNCHRONIZATION
=====================================================================

Feature                                               | Status | Notes
---------------------------------------------------------------------
Shared turn counter synchronized                      |  true  | reading this result is hard due to failing UI
Shared year counter synchronized                      |  true  | reading this result is hard due to failing UI
Only one action per turn enforced (lockstep)          |  true  | 
Year increments after 4 global turns                  |  true  |
Inactive players default to Skip Turn                 |  true  | should expose this for all instances to make testing easier


=====================================================================
4. KEEPER LOGIC (deterministic + migratory)
=====================================================================

Feature                                               | Status | Notes
---------------------------------------------------------------------
keeperTally initialized from lastResolvedRound        | false  |
Deterministic Keeper selection at turn 1              |  true  |
Host migration occurs at turn 1                       | false  |
keeperTally transferred old → new Keeper              | false  |
Keeper UI updates across all clients                  |  true  | The UI is a mess, but it updates


=====================================================================
5. TURN ACTIONS (End Turn / Skip Turn)
=====================================================================

Feature                                               | Status | Notes
---------------------------------------------------------------------
End Turn increments score + sets exhaustion=true      |  true  | 
Skip Turn no score change + exhaustion=false          |  true  | 
All players locked into same turn state               |  true  | 


=====================================================================
6. KEEPER VALIDITY CHECK (end of year)
=====================================================================

Feature                                               | Status | Notes
---------------------------------------------------------------------
Keeper pings all players; resets reachable inactive   | false  | none of this has been implemented
keeperTally updates correctly                         |  true  | 
Deterministic Keeper change across clients            |  true  | 
Keeper lastResolvedRound updates and syncs to clients |  true  | 
Clients update UI to show new Keeper                  | "true" | not validated because UI is so awful


=====================================================================
7. DISCONNECT HANDLING
=====================================================================

-- KEEPER disconnects --
Feature                                               | Status | Notes
---------------------------------------------------------------------
Game continues without crash/lock                     |  true  | replacement determined immediately
Remaining clients set Keeper isActive=false           | false  | the lost client loses it's instance in other game instances
UI displays Keeper disconnect                         | false  | impossible to determine because of UI mess
Next validity check assigns new Keeper (hostless)     | false  | none of this has been implemented

-- REGULAR player disconnects --
Feature                                               | Status | Notes
---------------------------------------------------------------------
Game continues without crash/lock                     |  true  | 
Remaining clients set player isActive=false           | false  | the lost client loses it's instance in other game instances


=====================================================================
8. MID-SESSION JOIN (optional)
=====================================================================

Feature                                               | Status | Notes
---------------------------------------------------------------------
New player joins and sees correct turn/year/Keeper    | false  | !exhaustion, turn 0, round 0 (these get corrected after the first update from host)
New player spawns as isActive=false                   | false  | spawns as active
